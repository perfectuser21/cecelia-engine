# PRD - 两阶段分离测试

## 目标

验证两阶段分离是否正确工作：

### 会话 1（P0）：
1. 创建包含类型错误的代码
2. 创建 PR
3. **立即结束会话（不等 CI）**

### 会话 2（P1）：
1. 用户问"怎么样了"
2. 检测到 PHASE=p1（CI 失败）
3. 进入轮询循环
4. 修复代码
5. 继续循环直到成功
6. 自动合并

## 成功标准

- [ ] P0: PR 创建后会话结束（不轮询 CI）
- [ ] P1: 下次询问时才检测 CI 状态
- [ ] P1: 进入轮询循环修复
- [ ] P1: 成功后自动合并
