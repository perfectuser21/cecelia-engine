# DoD - 修复 back-merge workflow 误触发问题

QA: docs/QA-DECISION.md

## 验收标准

### 功能验收
- [ ] workflow 文件添加了 `if: github.ref == 'refs/heads/main'` 条件到 check-trigger job
      Test: manual:检查 .github/workflows/back-merge-main-to-develop.yml 第 13 行
- [ ] 在非 main 分支 push 时，back-merge workflow 完全不运行（而非运行后 skip）
      Test: manual:push 到 develop 分支后检查 GitHub Actions 页面
- [ ] 在 main 分支 push 时，workflow 正常运行并检查触发条件
      Test: manual:将来合并到 main 后验证
- [ ] workflow 的其他逻辑（创建 PR、auto-merge）未被修改
      Test: manual:代码审查确认只修改了 if 条件

### 测试验收
- [ ] npm run qa 通过
      Test: contract:C2-001
- [ ] CI 通过，无新增失败
      Test: manual:等待 CI 运行完成
