# DoD - 修复 package-lock.json 版本不一致

## 验收标准

- [x] package-lock.json 版本与 package.json 一致 (8.0.4)
- [x] npm ci 可正常执行
- [x] npm run qa 通过

## 验证命令

```bash
# 检查版本一致性
cat package-lock.json | head -5

# 验证依赖安装
npm ci

# 质检
npm run qa
```

## 改动范围

- package-lock.json (版本号同步)
