# L2B Evidence: cp-fix-audit-issues

## 验证命令

```bash
# 检查 regression-contract.yaml 版本
grep "version:" regression-contract.yaml
# 结果: version: "12.4.6"

# 检查 CLAUDE.md hooks 列表
grep -A 8 "├── hooks/" CLAUDE.md | head -10
# 结果: 包含 (项目)/(全局)/[已废弃] 标注
```

## 手动验证

- version-check: regression-contract.yaml 版本已更新为 12.4.6
- hooks-list-check: CLAUDE.md hooks 列表标注了项目/全局/废弃状态
- hooks-example-check: CLAUDE.md 配置示例不包含 pr-gate-v2.sh

## 自动化测试

```bash
npm run test
# CI 验证
```

## 机器引用

- Branch: cp-fix-audit-issues
- Test: CI 验证
